name: stock-price-service CI

on:
  push:
    paths:
      - stock-price-service/**
  pull_request:
    paths:
      - stock-price-service/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "corretto"
          cache: maven
      - run: |
          cd stock-price-service
          mvn package
