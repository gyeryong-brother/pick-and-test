name: dividend-service CI

on:
  push:
    paths:
      - dividend-service/**
  pull_request:
    paths:
      - dividend-service/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "corretto"
          cache: maven
      - run: |
          cd dividend-service
          mvn package
